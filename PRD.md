# Roomly PRD — Ralph Loop用タスク一覧

## 概要
賃貸管理会社向け物件・入居者・契約・家賃管理SaaS。
現状: UI実装済み（9画面）、DB未デプロイ、認証モック状態。
このPRDはRalph Loopで自律実行するためのタスク一覧。

## 実行ルール
- 1タスク完了ごとにコミット
- CLAUDE.mdのルールに従う（日本語、確認不要、pushまで）
- 完了したタスクは `[x]` に変更
- 検証コマンドが通ったら完了とみなす

---

## Phase 1: バックエンド基盤（動くプロダクトにする）

### 1.1 Supabase デプロイ & RLS有効化
- [x] `supabase db push` でマイグレーション実行（既存Supabaseプロジェクトに対して）
- [x] 全テーブルのRLSを有効化（`ALTER TABLE ... ENABLE ROW LEVEL SECURITY`）
- [x] 各テーブルに `company_id = auth.jwt()->>'company_id'` のSELECT/INSERT/UPDATE/DELETEポリシー作成
- [x] JWT Hook（00002_custom_access_token_hook.sql）をSupabase Dashboardで有効化
- [x] シードデータ投入確認
- **検証**: Supabase Dashboard でテーブル一覧が表示 + RLSポリシーが各テーブルに存在
- **注意**: Supabase無料プラン上限（2アクティブ）のため、Luna HPのSupabaseを使用。新規プロジェクト作成不可

### 1.2 認証の本番化
- [x] LoginPage で `supabase.auth.signInWithPassword()` を呼び出す
- [x] サインアップフロー実装（会社名 + メール + パスワード）
- [x] サインアップ時に `companies` テーブルにレコード作成 + `users` テーブルに紐付け
- [x] `auth-context.tsx` のモック部分を実際のSupabase Auth に置き換え
- [x] ログアウト処理の実装
- [x] パスワードリセットフロー（メール送信）
- **検証**: `npm run build` 成功 + LoginPage にsupabase.auth呼び出しが存在

### 1.3 バリデーションライブラリ導入
- [ ] `zod` をインストール
- [ ] 物件スキーマ（name必須、address必須、units数値）
- [ ] 入居者スキーマ（name必須、email形式、phone形式）
- [ ] 契約スキーマ（start_date < end_date、rent > 0）
- [ ] 家賃請求スキーマ（amount > 0、billing_date必須）
- [ ] 修繕依頼スキーマ（title必須、priority enum）
- **検証**: `npm run build` 成功 + `src/lib/schemas.ts` が存在

### 1.4 物件CRUD
- [ ] 物件追加モーダル（名前・住所・オーナー選択・構造・築年数）
- [ ] 物件編集モーダル（既存データをprefill）
- [ ] 物件削除（確認ダイアログ付き、部屋・契約がある場合は警告）
- [ ] `POST /api/properties` — zodバリデーション + Supabase insert
- [ ] `PUT /api/properties/[id]` — zodバリデーション + Supabase update
- [ ] `DELETE /api/properties/[id]` — 関連データチェック + Supabase delete
- [ ] 成功時: モーダル閉じ + 一覧リフレッシュ。失敗時: エラーメッセージ表示
- **検証**: `npm run build` 成功 + 3つのAPIルートが存在

### 1.5 部屋CRUD
- [ ] 物件詳細ページに「部屋追加」ボタン + モーダル（部屋番号・面積・間取り・家賃）
- [ ] 部屋編集モーダル
- [ ] 部屋削除（入居者がいる場合は警告）
- [ ] `POST /api/units` — Supabase insert
- [ ] `PUT /api/units/[id]` — Supabase update
- [ ] `DELETE /api/units/[id]` — Supabase delete
- **検証**: `npm run build` 成功

### 1.6 入居者CRUD
- [ ] 入居者追加モーダル（名前・メール・電話・緊急連絡先）
- [ ] 入居者編集モーダル
- [ ] 入居者詳細画面（契約履歴・支払い履歴）
- [ ] APIルート（POST/PUT/DELETE）
- **検証**: `npm run build` 成功

### 1.7 契約CRUD
- [ ] 契約作成フロー（物件→部屋→入居者選択→期間・家賃・敷金・礼金）
- [ ] 契約更新（更新日・新家賃）
- [ ] 契約終了処理（退去日記録・敷金精算）
- [ ] APIルート
- **検証**: `npm run build` 成功

### 1.8 家賃管理 — 入金消込
- [ ] 月次家賃請求の自動生成ロジック（契約情報から毎月1日に生成）
- [ ] 入金消込UI（請求一覧から入金登録）
- [ ] 部分入金対応（一部入金 + 残額表示）
- [ ] 滞納アラート（支払期限超過で自動フラグ）
- [ ] 滞納一覧のフィルタリング（期間・物件・入居者）
- [ ] APIルート
- **検証**: `npm run build` 成功

### 1.9 修繕管理CRUD
- [ ] 修繕依頼登録モーダル（物件・部屋・カテゴリ・優先度・内容・写真）
- [ ] ステータス更新（受付→対応中→完了）
- [ ] 対応ログ追加（日時・内容・担当者・費用）
- [ ] APIルート
- **検証**: `npm run build` 成功

### 1.10 問い合わせCRUD
- [ ] 問い合わせ登録モーダル
- [ ] ステータス更新 + 対応ログ
- [ ] APIルート
- **検証**: `npm run build` 成功

---

## Phase 2: 機能拡張

### 2.1 オーナー送金計算
- [x] 月次送金計算ロジック（家賃収入 - 管理手数料 - 修繕費 - 経費）
- [x] 送金明細一覧UI
- [x] 送金ステータス管理（未送金→送金済み）
- [x] 送金明細PDF出力
- **検証**: `npm run build` 成功

### 2.2 経費管理CRUD
- [x] 経費登録モーダル（物件・カテゴリ・金額・日付・メモ・領収書）
- [x] 経費一覧のフィルタリング（期間・物件・カテゴリ）
- [x] 月次経費サマリー
- [x] APIルート
- **検証**: `npm run build` 成功

### 2.3 ダッシュボード実データ接続
- [ ] KPI（入居率・家賃回収率・空室数）を実データから算出
- [ ] アラート（滞納・契約満了30日前・修繕未対応）を実データから生成
- [ ] パイプライン（空室→内見→申込→契約）を実データ反映
- [ ] 月次推移グラフを実データで描画
- **検証**: `npm run build` 成功 + ダッシュボードにモックデータ参照が残っていない

### 2.4 検索・フィルタ・ソート
- [ ] 物件一覧: エリア・オーナー・入居率でフィルタ
- [ ] 入居者一覧: 名前・物件・ステータスで検索
- [ ] 契約一覧: ステータス・満了日でフィルタ + ソート
- [ ] 家賃一覧: 期間・ステータス・物件でフィルタ
- [ ] 各一覧にページネーション（20件/ページ）
- **検証**: `npm run build` 成功

### 2.5 CSV/PDFエクスポート
- [ ] 物件一覧CSV
- [ ] 入居者一覧CSV
- [ ] 家賃入金一覧CSV
- [ ] 月次レポートPDF（オーナー向け送金明細）
- **検証**: `npm run build` 成功

### 2.6 フリーミアム制御
- [ ] 現在の区画数カウント
- [ ] 10区画超過時にアップグレード促進モーダル
- [ ] 超過後は新規部屋追加をブロック（既存データの閲覧・編集は可能）
- **検証**: `npm run build` 成功

---

## Phase 3: 運用・セキュリティ

### 3.1 RBAC実装
- [ ] 4ロール定義（admin / manager / staff / viewer）
- [ ] ロール別アクセス制御（削除はadminのみ、閲覧はviewer以上等）
- [ ] RLS側でもロールチェック
- **検証**: `npm run build` 成功

### 3.2 通知機能
- [ ] 滞納発生時にメール通知（Resend）
- [ ] 契約満了30日前にリマインダーメール
- [ ] 修繕依頼登録時にオーナーへメール
- [ ] アプリ内通知ベル（ヘッダー）に未読件数表示
- **検証**: `npm run build` 成功

### 3.3 エラー監視
- [ ] Sentry導入（`@sentry/nextjs`）
- [ ] 全APIルートにエラー報告
- [ ] クライアント側エラーバウンダリ
- **検証**: `npm run build` 成功 + `sentry.client.config.ts` が存在

### 3.4 テスト
- [ ] Vitest導入 + 設定
- [ ] zodスキーマのユニットテスト
- [ ] 家賃計算ロジックのユニットテスト
- [ ] オーナー送金計算のユニットテスト
- **検証**: `npx vitest run` が全テスト PASS

---

## Phase 4: HP/LP & マーケティング

### 4.1 HP コンテンツ充実
- [ ] 機能紹介ページ（スクリーンショット付き）
- [ ] 料金ページ（フリーミアム + 有料プラン）
- [ ] 導入事例ページ（テンプレート、β後に実データ差し替え）
- [ ] FAQ
- **検証**: `npm run build` 成功（hp/ディレクトリ）

### 4.2 SEO記事公開
- [ ] draft/ の記事をレビュー・校正して released/ に移動
- [ ] 記事間の内部リンク設計
- [ ] 各記事にCTA（無料で始める/お問い合わせ）を設置
- **検証**: released/ に5本以上の記事が存在

### 4.3 LP 完成
- [ ] 業態別LP（小規模管理会社/中規模/大規模）
- [ ] フォーム送信 → Supabase inquiries テーブル保存
- [ ] Resendでサンキューメール送信
- **検証**: `npm run build` 成功（lp/ディレクトリ）
